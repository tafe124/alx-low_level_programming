include "main.h"
#include <stdio.h>

/**
 * add - adds two integers and returns the result(sum)
 * @i: print int i
 * @k: print int k
 *
 * Description: a function that adds two integers and returns the result
 *
 * Return: always 0.
 */

int add(int i, int k)
{
	return (i + k);
}
